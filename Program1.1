// Sarah Gilliland
// CSI 1044 - Halverson
// Program 1 - Sales Tax
// This program prompts the user for the purchase amount and prints out a
// recept which includes the purchase amount, state sales tax,
// city sales tax and total sales tax in a specific format.

#include <iostream>
#include <fstream>
#include <iomanip>

using namespace std;

int main(void)
{
	// Defining my variables
	int count;
	double PAmount, STax, CTax, TotalAmount;
	const double STaxPercent = 0.05, CTaxPercent = 0.032;
		count = 1;
	// Create the outfile
	ofstream outfile;
	outfile.open("Test.txt");
	outfile << "Sarah Gilliland\n\n";
	// Setting the decimals
	outfile << fixed << setprecision(2);
	// Looping the program for 10 purchase reports
	while (count <= 10)
	{
		//Prompting my user for the purchase amount
		cout << "Please enter the purchase amount: ";
		cin >> PAmount;
		// Program statements
		STax = PAmount * STaxPercent;
		CTax = PAmount * CTaxPercent;
		TotalAmount = PAmount + STax + CTax;
		outfile << left << setw(24) << "\nPurchase amount" << right << "$" 
			<< setw(9) << PAmount << '\n';
		outfile << left << setw(23) << "State sales tax" << right << "$" 
			<< setw(9) << STax << '\n';
		outfile << left << setw(23) << "City sales tax" << right << "$" 
			<< setw(9) << CTax << '\n';
		outfile << setw(35) << "-------------\n";
			outfile << left << setw(23) << "Total sales amount" << right 
				<< "$" << setw(9) << TotalAmount << '\n';
		count += 1;
	}
	// End the program
	system("pause");
	return 0;
}
